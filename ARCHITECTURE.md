# 🏗 Архитектура проекта

## 📋 Общая информация

**Стек**: Next.js 14 (App Router) + TypeScript + Tailwind CSS + Framer Motion

**Цель**: Многостраничный сайт строительной компании, оптимизированный под Яндекс.Директ с акцентом на конверсию

---

## 🗂 Структура файлов

```
d:\Stroy/
├── src/
│   ├── app/                       # App Router (Next.js 14)
│   │   ├── layout.tsx             # Root Layout (Header + Footer)
│   │   ├── page.tsx               # Главная страница
│   │   ├── globals.css            # Глобальные стили
│   │   ├── not-found.tsx          # 404 страница
│   │   │
│   │   ├── services/
│   │   │   ├── page.tsx           # Список всех услуг
│   │   │   └── [slug]/
│   │   │       └── page.tsx       # Динамическая страница услуги
│   │   │
│   │   ├── portfolio/
│   │   │   └── page.tsx           # Портфолио
│   │   │
│   │   ├── blog/
│   │   │   └── page.tsx           # Блог
│   │   │
│   │   ├── about/
│   │   │   └── page.tsx           # О компании
│   │   │
│   │   └── contacts/
│   │       └── page.tsx           # Контакты
│   │
│   ├── components/
│   │   ├── layout/                # Общий layout
│   │   │   ├── Header.tsx         # Шапка с меню
│   │   │   └── Footer.tsx         # Футер
│   │   │
│   │   ├── home/                  # Компоненты главной
│   │   │   ├── HomeHero.tsx       # Hero-секция
│   │   │   ├── ServicesGrid.tsx   # Сетка услуг
│   │   │   ├── WhyUs.tsx          # Преимущества
│   │   │   ├── StatsSection.tsx   # Статистика
│   │   │   └── CTASection.tsx     # Призыв к действию
│   │   │
│   │   ├── services/              # Компоненты страниц услуг
│   │   │   ├── ServiceHero.tsx    # Hero с формой
│   │   │   ├── ProblemsSection.tsx # Проблемы → Решения
│   │   │   ├── ProcessSection.tsx  # Процесс работы
│   │   │   ├── PricingSection.tsx  # Таблица цен
│   │   │   ├── CasesSection.tsx    # Кейсы
│   │   │   └── FAQSection.tsx      # FAQ (Schema.org)
│   │   │
│   │   ├── calculator/            # Калькуляторы
│   │   │   ├── Calculator.tsx     # Обёртка
│   │   │   ├── PlasterCalculator.tsx   # Штукатурка
│   │   │   ├── FacadeCalculator.tsx    # Фасады
│   │   │   └── SignageCalculator.tsx   # Вывески
│   │   │
│   │   ├── forms/
│   │   │   └── ContactForm.tsx    # Форма обратной связи
│   │   │
│   │   ├── portfolio/
│   │   │   ├── PortfolioHero.tsx
│   │   │   └── PortfolioGrid.tsx  # Сетка с фильтрацией
│   │   │
│   │   └── blog/
│   │       ├── BlogHero.tsx
│   │       └── BlogList.tsx       # Список статей
│   │
│   ├── data/
│   │   └── services.ts            # Конфигурация услуг
│   │
│   └── lib/
│       └── utils.ts               # Утилиты
│
├── package.json                   # Зависимости
├── tsconfig.json                  # TypeScript config
├── tailwind.config.ts             # Tailwind config
├── next.config.js                 # Next.js config
├── README.md                      # Полная документация
└── QUICKSTART.md                  # Быстрый старт
```

---

## 🎯 Ключевые архитектурные решения

### 1. **Динамическая генерация страниц услуг**

**Проблема**: Нужно 3+ уникальных страницы для рекламы с разными заголовками и контентом

**Решение**:
- Централизованный файл `services.ts` с данными всех услуг
- Динамический маршрут `/services/[slug]`
- `generateStaticParams()` для pre-rendering при билде
- `generateMetadata()` для уникальных title/description на каждой странице

**Преимущества**:
- Добавление новой услуги = 1 объект в `services.ts`
- Автоматическое создание страницы, меню, SEO
- Консистентная структура контента

### 2. **Модульная компонентная архитектура**

**Принцип**: Каждая секция = отдельный переиспользуемый компонент

**Пример страницы услуги**:
```tsx
<ServiceHero {...service} />
<ProblemsSection {...} />
<Calculator type={service.calculatorType} />
<ProcessSection steps={service.processSteps} />
<PricingSection pricing={service.pricing} />
<CasesSection cases={service.cases} />
<FAQSection faqs={service.faq} />
```

**Преимущества**:
- Легко менять порядок секций
- Переиспользование компонентов
- Простота тестирования

### 3. **Конфигурационный подход к контенту**

Вместо хардкода в компонентах, весь контент в `data/services.ts`:

```typescript
export interface ServiceData {
  title: string
  priceFrom: number
  benefits: string[]
  problems: string[]
  solutions: string[]
  processSteps: ProcessStep[]
  pricing: PricingItem[]
  faq: FAQItem[]
  // ... и т.д.
}
```

**Преимущества**:
- Контент-менеджер может редактировать без знания React
- Легко мигрировать в CMS (Strapi, Contentful)
- Типизация предотвращает ошибки

### 4. **Типизированные калькуляторы**

Три специализированных калькулятора для разных услуг:

- **PlasterCalculator**: площадь × толщина + потолки
- **FacadeCalculator**: материал × площадь + утепление
- **SignageCalculator**: тип × количество × размер

Выбор калькулятора через `calculatorType` в `services.ts`.

### 5. **SEO-первая архитектура**

**Реализовано**:
- ✅ Server Components по умолчанию
- ✅ `generateMetadata()` для каждой страницы
- ✅ Schema.org разметка в FAQ (`itemProp`, `itemScope`)
- ✅ Семантический HTML (h1, h2, section, article)
- ✅ Уникальные title/description для каждой услуги

**Что добавить**:
- [ ] `sitemap.xml` (через `next-sitemap`)
- [ ] JSON-LD для Organization
- [ ] Breadcrumbs разметка

### 6. **Конверсионный дизайн**

**Паттерны**:
- Формы в Hero-секциях (первый экран)
- Контрастные CTA (оранжевый на тёмном)
- Социальные доказательства (цифры, кейсы)
- Прозрачное ценообразование
- Снятие возражений (FAQ)

**Цветовая схема**:
- `primary` (#2A2D34) — индустриальный антрацит
- `accent.orange` (#FF6B35) — призыв к действию
- `accent.blue` (#0077C8) — альтернативный акцент

---

## 🔄 Data Flow

```
services.ts (данные)
      ↓
/services/[slug]/page.tsx (генерация страницы)
      ↓
Service Components (Hero, Process, FAQ...)
      ↓
UI (отображение с анимациями)
```

---

## 🚀 Performance оптимизации

1. **Server Components** — минимум JS на клиенте
2. **Static Generation** — страницы pre-render при билде
3. **Image optimization** — настроен `next/image` (webp, avif)
4. **Tree-shaking** — optimizePackageImports для lucide-react
5. **CSS-in-JS** — Tailwind (purge неиспользуемых стилей)

---

## 📱 Адаптивность

**Breakpoints** (Tailwind defaults):
- `sm:` — 640px+ (мобильные горизонтально)
- `md:` — 768px+ (планшеты)
- `lg:` — 1024px+ (ноутбуки)
- `xl:` — 1280px+ (десктопы)

**Стратегия**: Mobile-first + progressive enhancement

---

## 🧪 Расширяемость

### Добавление новой услуги

1. Добавить объект в `services.ts`
2. Опционально: создать новый калькулятор
3. Готово! Страница создастся автоматически

### Интеграция CMS

Заменить `services.ts` на fetch из:
- Strapi
- Contentful
- Sanity
- WordPress REST API

### Backend для форм

Варианты:
- **Telegram Bot** (простейший)
- **Email API** (SendGrid, Mailgun)
- **CRM webhook** (amoCRM, Bitrix24)

---

## 📊 Метрики для отслеживания

**Конверсионные цели**:
- Отправка форм (hero, контакты)
- Клики на телефон
- Использование калькулятора
- Переходы на страницы услуг

**SEO**:
- Органический трафик на /services/[slug]
- Позиции по ключам (механизированная штукатурка, вентфасады)
- CTR в поисковой выдаче

---

## 🎓 Примеры использования

### Редактирование цены услуги

```typescript
// src/data/services.ts
{
  slug: 'mekhanizirovannaya-shtukaturka',
  priceFrom: 350, // было 320
  // ...
}
```

### Добавление нового этапа в процесс

```typescript
processSteps: [
  // ... существующие
  {
    title: 'Финальный осмотр',
    description: 'Приёмка работ заказчиком',
    duration: '0.5 дня',
  },
]
```

### Изменение контактов

```typescript
// src/components/layout/Header.tsx
const COMPANY_PHONE = '+79001234567'

// src/components/layout/Footer.tsx
const COMPANY_INFO = {
  phone: '+79001234567',
  email: 'info@stroy-company.ru',
  // ...
}
```

---

## ✅ Чек-лист перед запуском

- [ ] Заменить placeholder телефоны/email на реальные
- [ ] Добавить реальные изображения услуг и проектов
- [ ] Настроить отправку форм (webhook/email)
- [ ] Подключить Яндекс.Метрику
- [ ] Проверить мобильную версию на реальных устройствах
- [ ] Настроить SSL сертификат
- [ ] Добавить `sitemap.xml`
- [ ] Настроить `robots.txt`

---

**Архитектура спроектирована под масштабирование и легкую поддержку** ✨
