# 🚀 Деплой на Timeweb Cloud - Финальная инструкция

## 📋 Информация о проекте

**Проект:** Golden House - сайт строительной компании  
**Технологии:** Next.js 14 + TypeScript + Tailwind CSS  
**Тип деплоя:** Статический экспорт (Static Site Generation)  
**Репозиторий:** https://github.com/Tips95/golden_house  
**Ветка:** `main`  

---

## ⚙️ Настройка приложения в Timeweb Cloud

### 1. Создание приложения

1. Войдите в панель управления [Timeweb Cloud](https://timeweb.cloud/)
2. Перейдите в раздел **"App Platform"** → **"Приложения"**
3. Нажмите **"Создать приложение"**
4. Выберите **"Из Git-репозитория"**

### 2. Подключение репозитория

```
Repository URL: https://github.com/Tips95/golden_house.git
Branch: main
```

✅ **Включите автодеплой** - приложение будет автоматически пересобираться при каждом `git push`

### 3. Build Settings (Настройки сборки)

#### Тип приложения
```
Next.js
```

#### Версия Node.js
```
20.x (или выше)
```

#### Package Manager
```
npm
```

#### Install Command
```bash
npm install
```

#### Build Command
```bash
(ОСТАВИТЬ ПУСТЫМ)
```

⚠️ **КРИТИЧНО:** Поле **Build Command** должно быть **ПУСТЫМ**!

#### Start Command
```bash
npm run build && npm start
```

**Что происходит:**
- Выполняется `npm run build` (создает директорию `.next`)
- Сразу после этого выполняется `npm start`
- Запускает Next.js production сервер
- Использует только что созданную директорию `.next`
- Слушает на порту из переменной окружения `PORT` (автоматически передается Timeweb)

#### Output Directory (Директория сборки)
```
Не указывать (оставить по умолчанию)
```

Next.js создает папку `.next/` с оптимизированными файлами для production сервера. Эта папка создается при выполнении `npm run build` в Start Command.

### 4. Environment Variables (Переменные окружения)

Добавьте следующие переменные в панели Timeweb Cloud:

| Переменная | Значение | Обязательная |
|------------|----------|--------------|
| `NODE_ENV` | `production` | ✅ Да |
| `NEXT_PUBLIC_SITE_URL` | `https://ваш-домен.twc1.net` | ✅ Да |

**Опционально (для аналитики):**

| Переменная | Значение | Обязательная |
|------------|----------|--------------|
| `NEXT_PUBLIC_YANDEX_METRIKA_ID` | ваш_id | ❌ Нет |
| `NEXT_PUBLIC_GOOGLE_ANALYTICS_ID` | ваш_id | ❌ Нет |

⚠️ **ВАЖНО:** Переменная `PORT` НЕ НУЖНА - Timeweb автоматически передаст её при запуске!

### 5. Region Settings (Регион)

Рекомендуется выбрать:
```
Москва (ru-1)
```

---

## 🔄 Процесс автодеплоя

После каждого `git push` в ветку `main`:

1. ✅ Timeweb получает webhook от GitHub
2. ✅ Выполняется `git pull` (получение изменений)
3. ✅ Выполняется `npm install` (установка зависимостей)
4. ✅ Выполняется `npm run build && npm start` (сборка и запуск)
   - `npm run build` создает директорию `.next` с собранным приложением
   - Next.js генерирует статические страницы
   - `npm start` запускает Next.js production сервер
   - Слушает на порту из переменной окружения `PORT` (автоматически передается Timeweb)
5. ✅ Сайт становится доступен по домену

**Время деплоя:** 2-5 минут

---

## 📊 Ожидаемые логи при успешном деплое

### Фаза установки
```
> npm install
added 393 packages in 15s
```

### Фаза сборки
```
> npm run build
✓ Creating optimized production build
✓ Compiled successfully
✓ Generating static pages (24/24)

Route (app)                                   Size     First Load JS
┌ ○ /                                         8.5 kB   154 kB
├ ○ /about                                    2.1 kB   148 kB
├ ○ /blog                                     1.8 kB   147 kB
├ ○ /contacts                                 3.2 kB   149 kB
├ ○ /portfolio                                2.5 kB   148 kB
├ ○ /services                                 175 B    96.2 kB
└ ● /services/[slug]                          9.85 kB  133 kB

○  (Static)  prerendered as static content
●  (SSG)     prerendered as static HTML (uses getStaticProps)
```

### Фаза запуска
```
> npm start
> next start

▲ Next.js 14.2.35
- Local:        http://localhost:3000
- Network:      http://0.0.0.0:3000

✓ Ready in 1.2s
```

✅ Если видите `Ready` - сервер запущен успешно!

---

## ✅ Проверка после деплоя

### Основные страницы

- ✅ Главная: `/`
- ✅ Услуги: `/services`
- ✅ Конкретная услуга: `/services/mekhanizirovannaya-shtukaturka`
- ✅ Портфолио: `/portfolio`
- ✅ Контакты: `/contacts`
- ✅ О компании: `/about`
- ✅ Блог: `/blog`

### Функциональность

- ✅ Навигация работает
- ✅ WhatsApp кнопки открываются
- ✅ Калькуляторы функционируют
- ✅ Мобильная версия адаптивна
- ✅ Стили загружаются корректно
- ✅ Изображения отображаются

---

## 🐛 Troubleshooting (Решение проблем)

### ❌ Проблема: "Could not find a production build in the '.next' directory"

**Причина:** Build Command не пустой, или Start Command не содержит `npm run build`

**Решение:**
1. Убедитесь, что Build Command **ПУСТОЙ**
2. Start Command должен быть: `npm run build && npm start`
3. Пересоберите приложение: **App Platform** → **Пересобрать**

---

### ❌ Проблема: Сайт не открывается (404)

**Возможные причины:**
1. Сборка завершилась с ошибками
2. Неправильная конфигурация команд

**Решение:**
1. Проверьте логи сборки - должно быть `✓ Generating static pages (24/24)`
2. Убедитесь, что команды настроены правильно (Build пустой, Start = `npm run build && npm start`)
3. Пересоберите приложение: **App Platform** → **Пересобрать**

---

### ❌ Проблема: Стили не загружаются

**Причина:** Неправильный базовый URL

**Решение:**
1. Проверьте переменную окружения `NEXT_PUBLIC_SITE_URL` - она должна совпадать с вашим доменом
2. Убедитесь, что в `next.config.js` установлено `images: { unoptimized: true }`

---

### ❌ Проблема: Ошибки TypeScript при сборке

**Решение:**
```bash
# Локально выполните проверку типов
npm run type-check

# Исправьте все ошибки
# Затем закоммитьте и запушьте
git add .
git commit -m "Fix TypeScript errors"
git push origin main
```

---

## 🔄 Обновление сайта

### Локальная разработка

```bash
# 1. Внесите изменения в код
# ...

# 2. Проверьте локально
npm run dev

# 3. Соберите проект
npm run build

# 4. Проверьте сборку
npm start

# 5. Закоммитьте изменения
git add .
git commit -m "Описание изменений"

# 6. Отправьте в GitHub
git push origin main
```

### Автоматический деплой

После `git push origin main` Timeweb Cloud автоматически:
1. Обнаружит изменения через webhook
2. Выполнит новую сборку
3. Перезапустит приложение

**Время автодеплоя:** 2-5 минут

---

## 📈 Производительность

### Оптимизации (уже настроены)

- ✅ **Next.js 14** - современный App Router
- ✅ **Static Site Generation** - максимальная скорость
- ✅ **Статические страницы** - 24 предсгенерированных страницы
- ✅ **Компрессия** - автоматическая в production
- ✅ **Минификация** - встроенная в Next.js
- ✅ **Tree Shaking** - удаление неиспользуемого кода

### Ожидаемые метрики Google Lighthouse

- ⚡ **Performance:** > 90
- ⚡ **Accessibility:** > 95
- ⚡ **Best Practices:** > 95
- ⚡ **SEO:** > 95

---

## 🔐 Безопасность

### Что настроено

- ✅ Переменные окружения защищены
- ✅ `.env` файлы не в репозитории (`.gitignore`)
- ✅ HTTPS автоматически от Timeweb Cloud
- ✅ Security headers в Next.js
- ✅ `poweredByHeader: false` (скрыт X-Powered-By)
- ✅ Strict Mode включен

---

## 📞 Поддержка

### Timeweb Cloud

- 🌐 **Сайт:** https://timeweb.cloud/
- 📚 **Документация:** https://timeweb.cloud/help/
- 📚 **Next.js гайд:** https://timeweb.cloud/docs/apps/deploying-frontend-apps/nextjs
- 💬 **Тикеты:** Личный кабинет → Поддержка
- ☎️ **Телефон:** 8 (800) 700-32-92

### Компания Golden House

- 📧 **Email:** Golden.House.Services@mail.ru
- 📱 **WhatsApp:** +7 (928) 195-88-85

---

## ✅ Чеклист готовности к деплою

Перед первым деплоем убедитесь:

- [x] Git репозиторий настроен на GitHub
- [x] Все файлы закоммичены и запушены в ветку `main`
- [x] `package.json` содержит правильную команду `start`: `"next start"`
- [x] `next.config.js` настроен для работы с `next start` (без `output: 'export'`)
- [x] В Timeweb Cloud Build Command = ПУСТОЙ
- [x] В Timeweb Cloud Start Command = `npm run build && npm start`
- [x] Изображения настроены: `images: { unoptimized: true }`
- [x] `.gitignore` исключает `node_modules`, `.next`, `out`, `.env`
- [x] Проект собирается без ошибок: `npm run build`
- [x] TypeScript проверка проходит: `npm run type-check`

---

## 🎯 Быстрый старт

```bash
# 1. Убедитесь что все изменения закоммичены
git status

# 2. Если есть изменения - закоммитьте
git add .
git commit -m "Prepare for Timeweb Cloud deploy"

# 3. Отправьте в GitHub
git push origin main

# 4. Перейдите в панель Timeweb Cloud
# 5. Создайте новое приложение из Git-репозитория
# 6. Укажите настройки из раздела "Настройка приложения"
# 7. Нажмите "Создать и развернуть"
# 8. Дождитесь завершения деплоя (2-5 минут)
# 9. Откройте сайт по предоставленному домену
```

---

## 🎉 Готово!

После выполнения всех шагов:

- ✅ Сайт будет доступен по домену от Timeweb Cloud
- ✅ При каждом `git push` сайт будет автоматически обновляться
- ✅ HTTPS настроен автоматически
- ✅ CDN и кэширование работают из коробки

**Успешного деплоя!** 🚀
