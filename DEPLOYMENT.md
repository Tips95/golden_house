# 🚀 Инструкция по деплою на TimeWeb Cloud

## Предварительные требования

- Аккаунт на [TimeWeb Cloud](https://timeweb.cloud/)
- Node.js 18.x или выше
- Git репозиторий с проектом

---

## Шаг 1: Подготовка проекта

### 1.1 Установите зависимости локально
```bash
npm install
```

### 1.2 Проверьте сборку проекта
```bash
npm run build
npm run start
```

Откройте http://localhost:3000 и убедитесь, что сайт работает корректно.

---

## Шаг 2: Создание сервера на TimeWeb Cloud

### 2.1 Войдите в панель TimeWeb Cloud
1. Перейдите на https://timeweb.cloud/
2. Войдите в личный кабинет

### 2.2 Создайте облачный сервер
1. Нажмите **"Создать сервер"**
2. Выберите конфигурацию:
   - **ОС**: Ubuntu 22.04 LTS
   - **Тариф**: Минимум 1 CPU, 2 GB RAM, 10 GB SSD
   - **Регион**: Ближайший к вашей аудитории (Москва/Амстердам)
3. Создайте SSH-ключ или используйте пароль

---

## Шаг 3: Настройка сервера

### 3.1 Подключитесь к серверу по SSH
```bash
ssh root@ВАШ_IP_АДРЕС
```

### 3.2 Обновите систему
```bash
apt update && apt upgrade -y
```

### 3.3 Установите Node.js 20.x
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs
node -v  # Проверка версии
npm -v   # Проверка версии npm
```

### 3.4 Установите PM2 (менеджер процессов)
```bash
npm install -g pm2
```

### 3.5 Установите Nginx
```bash
apt install -y nginx
```

---

## Шаг 4: Загрузка проекта

### Вариант A: Через Git (рекомендуется)

```bash
# Установите Git
apt install -y git

# Клонируйте репозиторий
cd /var/www
git clone https://github.com/ВАШ_USERNAME/goldenhouse-services.git
cd goldenhouse-services

# Установите зависимости
npm install

# Создайте production build
npm run build
```

### Вариант B: Через FTP/SFTP

1. Используйте FileZilla или WinSCP
2. Подключитесь к серверу (IP, порт 22, пользователь root)
3. Загрузите все файлы в `/var/www/goldenhouse-services`
4. Выполните на сервере:
```bash
cd /var/www/goldenhouse-services
npm install
npm run build
```

---

## Шаг 5: Настройка переменных окружения

```bash
cd /var/www/goldenhouse-services
nano .env.production
```

Добавьте:
```env
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_SITE_URL=https://ваш-домен.ru
```

Сохраните (Ctrl+O, Enter, Ctrl+X)

---

## Шаг 6: Запуск приложения с PM2

```bash
cd /var/www/goldenhouse-services

# Запустите приложение
pm2 start ecosystem.config.js

# Проверьте статус
pm2 status

# Просмотр логов
pm2 logs goldenhouse-services

# Автозапуск при перезагрузке сервера
pm2 startup
pm2 save
```

---

## Шаг 7: Настройка Nginx

### 7.1 Создайте конфигурацию Nginx
```bash
nano /etc/nginx/sites-available/goldenhouse-services
```

Добавьте:
```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    # Оптимизация для статических файлов Next.js
    location /_next/static/ {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 200 60m;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Оптимизация для изображений
    location /images/ {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 200 30d;
        add_header Cache-Control "public, max-age=2592000";
    }
}
```

### 7.2 Активируйте конфигурацию
```bash
ln -s /etc/nginx/sites-available/goldenhouse-services /etc/nginx/sites-enabled/
nginx -t  # Проверка конфигурации
systemctl restart nginx
```

---

## Шаг 8: Настройка SSL (Let's Encrypt)

### 8.1 Установите Certbot
```bash
apt install -y certbot python3-certbot-nginx
```

### 8.2 Получите SSL-сертификат
```bash
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
```

Следуйте инструкциям на экране.

### 8.3 Автоматическое обновление сертификата
```bash
certbot renew --dry-run
```

---

## Шаг 9: Настройка DNS

1. Войдите в панель управления вашим доменом
2. Добавьте A-запись:
   - **Имя**: @ (или оставьте пустым)
   - **Тип**: A
   - **Значение**: IP_АДРЕС_ВАШЕГО_СЕРВЕРА
   - **TTL**: 3600

3. Добавьте запись для www:
   - **Имя**: www
   - **Тип**: CNAME
   - **Значение**: ваш-домен.ru
   - **TTL**: 3600

Подождите 5-30 минут для распространения DNS.

---

## Шаг 10: Проверка работы сайта

1. Откройте браузер
2. Перейдите на https://ваш-домен.ru
3. Проверьте работу всех страниц:
   - Главная
   - Услуги
   - Портфолио
   - Контакты
   - Кнопки WhatsApp

---

## Полезные команды PM2

```bash
# Просмотр статуса
pm2 status

# Перезапуск приложения
pm2 restart goldenhouse-services

# Остановка приложения
pm2 stop goldenhouse-services

# Просмотр логов
pm2 logs goldenhouse-services

# Очистка логов
pm2 flush

# Мониторинг ресурсов
pm2 monit
```

---

## Обновление сайта

```bash
cd /var/www/goldenhouse-services

# Получите последние изменения
git pull origin main

# Установите новые зависимости (если есть)
npm install

# Пересоберите проект
npm run build

# Перезапустите приложение
pm2 restart goldenhouse-services
```

---

## Мониторинг и логи

### Просмотр логов приложения
```bash
pm2 logs goldenhouse-services --lines 100
```

### Просмотр логов Nginx
```bash
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### Проверка использования ресурсов
```bash
pm2 monit
htop
```

---

## Решение проблем

### Сайт не открывается
1. Проверьте статус PM2: `pm2 status`
2. Проверьте логи: `pm2 logs`
3. Проверьте Nginx: `systemctl status nginx`
4. Проверьте порт: `netstat -tulpn | grep 3000`

### Ошибка 502 Bad Gateway
1. Убедитесь, что приложение запущено: `pm2 status`
2. Проверьте порт в Nginx конфигурации
3. Перезапустите приложение: `pm2 restart goldenhouse-services`

### SSL сертификат не работает
1. Проверьте конфигурацию Nginx: `nginx -t`
2. Обновите сертификат: `certbot renew`
3. Перезапустите Nginx: `systemctl restart nginx`

---

## Контакты для поддержки

- **Email**: Golden.House.Services@mail.ru
- **WhatsApp**: +7 (928) 195-88-85
- **Документация TimeWeb**: https://timeweb.cloud/help/

---

✅ **Ваш сайт готов к работе!**
