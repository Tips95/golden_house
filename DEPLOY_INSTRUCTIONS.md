# 🚀 Инструкция по деплою на Timeweb Cloud

## ✅ Что исправлено

1. ✅ Обновлен `.timeweb.config` - Build Command = `npm run build`, Start Command = `npm start`
2. ✅ Обновлена документация для единообразия
3. ✅ Проверена сборка проекта - работает без ошибок
4. ✅ Проверены TypeScript типы - ошибок нет
5. ✅ Проверен линтер - ошибок нет
6. ✅ `next.config.js` настроен для работы с `next start` (без `output: 'export'`)

## 📋 Требования Timeweb Cloud

Согласно официальной документации Timeweb Cloud:

- ✅ Git репозиторий (GitHub, GitLab, Bitbucket)
- ✅ Next.js проект с `package.json`
- ✅ Node.js 20.x или выше
- ✅ Правильные команды build/start
- ✅ Переменные окружения для production

## ⚙️ Настройки для панели Timeweb Cloud

### 1. Repository Settings

```
Repository URL: https://github.com/Tips95/golden_house.git
Branch: main
☑️ Включить автодеплой
```

### 2. Build Settings

```
Тип приложения:     Next.js
Node.js версия:     20.x
Package Manager:    npm
Root Directory:     /
```

### 3. Commands (КРИТИЧНО!)

```bash
Install Command:   npm install
Build Command:     (ОСТАВИТЬ ПУСТЫМ!)
Start Command:     npm run build && npm start
```

⚠️ **ВАЖНО:** 
- Поле **Build Command** должно быть **ПУСТЫМ**!
- Вся логика в команде **Start Command**: `npm run build && npm start`
- Это необходимо, потому что Timeweb Cloud не сохраняет `.next` между этапами Build и Start

### 4. Environment Variables

Добавьте в панели Timeweb Cloud:

```env
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://ваш-домен.twc1.net
```

*(Замените URL на ваш реальный домен от Timeweb)*

⚠️ **Примечание:** Переменная `PORT` НЕ НУЖНА - Timeweb автоматически передаст её при запуске!

## 🔄 Процесс деплоя

1. **Запушите изменения в GitHub:**
   ```bash
   git add .
   git commit -m "Configure for Timeweb Cloud deployment"
   git push origin main
   ```

2. **Создайте приложение в Timeweb Cloud:**
   - Войдите в [Timeweb Cloud](https://timeweb.cloud/)
   - **App Platform** → **Создать приложение**
   - Выберите **Из Git-репозитория**
   - Подключите репозиторий: `https://github.com/Tips95/golden_house.git`
   - Ветка: `main`
   - ✅ Включите **автодеплой**

3. **Настройте команды:**
   - Install Command: `npm install`
   - Build Command: **(оставить пустым)**
   - Start Command: `npm run build && npm start`

4. **Добавьте переменные окружения:**
   - `NODE_ENV=production`
   - `NEXT_PUBLIC_SITE_URL=https://ваш-домен.twc1.net`

5. **Запустите деплой:**
   - Нажмите **"Создать и развернуть"**
   - Дождитесь завершения (2-5 минут)

## ✅ Ожидаемые логи при успешном деплое

```
=== Install Phase ===
npm install
added 393 packages in 20s
✅ Install complete

=== Build Phase ===
npm run build

> next build
✓ Compiled successfully
✓ Generating static pages (24/24)
✅ Build complete

=== Start Phase ===
npm start

> next start
▲ Next.js 14.2.35
- Local:    http://localhost:3000
- Network:  http://0.0.0.0:3000

✓ Ready in 1.5s
✅ Server started

=== Deploy Complete ===
Your app is running at:
https://ваш-домен.twc1.net
```

## 🐛 Troubleshooting

### Ошибка: "Could not find a production build in the '.next' directory"

**Причина:** Build Command не пустой, или Start Command не содержит `npm run build`

**Решение:**
1. Убедитесь, что Build Command **ПУСТОЙ**
2. Start Command должен быть: `npm run build && npm start`
3. Пересоберите приложение: **App Platform** → **Пересобрать**

### Ошибка: "Port 3000 is already in use"

**Причина:** Переменная PORT не передается

**Решение:**
- Timeweb автоматически передает `PORT`
- Если проблема не исчезает, добавьте в переменные окружения: `PORT=3000`

### Сайт не открывается (404)

**Решение:**
1. Проверьте логи сборки - должно быть `✓ Generating static pages (24/24)`
2. Убедитесь, что команды настроены правильно
3. Пересоберите приложение

## 📚 Дополнительная документация

- **TIMEWEB_FINAL_SOLUTION.md** - подробное объяснение решения
- **QUICKSTART_TIMEWEB.md** - быстрый старт
- **DEPLOY.md** - полная документация
- **.timeweb.config** - конфигурация для копирования

## 🎉 После успешного деплоя

- ✅ Сайт доступен по домену от Timeweb Cloud
- ✅ HTTPS настроен автоматически
- ✅ При каждом `git push` сайт автоматически обновляется
- ✅ Все 24 страницы сгенерированы и работают

## 📞 Поддержка

**Timeweb Cloud:**
- 🌐 https://timeweb.cloud/
- 📚 https://timeweb.cloud/help/
- ☎️ 8 (800) 700-32-92

**Golden House:**
- 📧 Golden.House.Services@mail.ru
- 📱 +7 (928) 195-88-85

---

**Успешного деплоя!** 🚀
