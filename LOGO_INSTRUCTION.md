# 📸 Инструкция по добавлению логотипа GoldenHouse

## Что уже сделано

✅ Header обновлён — логотип вместо иконки "S"  
✅ Footer обновлён — логотип вместо иконки "S"  
✅ Название компании изменено на **GoldenHouse Services** во всех файлах  
✅ Email обновлён на `info@goldenhouse-services.ru`  
✅ Metadata обновлены (title, description, siteName)

---

## ⚡ Что нужно сделать

### 1. Сохранить логотип

**Сохраните изображение логотипа** (которое вы прислали) в папку:

```
d:\Stroy\public\images\
```

**Название файла должно быть:**
```
logo-goldenhouse.png
```

### 2. Полный путь к файлу

После сохранения у вас должен быть файл:
```
d:\Stroy\public\images\logo-goldenhouse.png
```

### 3. Запустить проект

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) — логотип появится в Header и Footer.

---

## 🎨 Если у вас другой формат

Если ваш логотип в формате `.svg` или `.jpg`, переименуйте его в:
- `.png` формат предпочтителен
- Или обновите импорты в файлах ниже

### Файлы с импортами логотипа:

1. **Header:**
   ```
   src/components/layout/Header.tsx
   ```
   Найдите строку:
   ```tsx
   src="/images/logo-goldenhouse.png"
   ```

2. **Footer:**
   ```
   src/components/layout/Footer.tsx
   ```
   Найдите строку:
   ```tsx
   src="/images/logo-goldenhouse.png"
   ```

Измените расширение на ваше (например, `.svg` или `.jpg`).

---

## 📏 Размеры логотипа

Текущие настройки:
- **Header**: 180×60px (автоматически масштабируется по высоте до 48px)
- **Footer**: 180×60px (белый вариант через CSS фильтры)

Если логотип выглядит слишком большим/маленьким, отредактируйте параметры:

```tsx
// В Header.tsx
<Image
  src="/images/logo-goldenhouse.png"
  width={180}    // ← измените ширину
  height={60}    // ← измените высоту
  className="h-12 w-auto"  // ← или измените класс
/>
```

---

## ✨ Готово!

После сохранения логотипа проект полностью обновлён под бренд **GoldenHouse Services**.
