# 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА: 404 на Timeweb Cloud

## Диагностика проблемы

В логах видно:
- ✅ `Build succeeded` - сборка успешна
- ✅ `Unpacking static` - статические файлы распакованы
- ❌ **НЕТ логов запуска приложения** (`npm start`)
- ❌ Timeweb думает, что это статический сайт, а не Node.js приложение

## ⚠️ ПРОБЛЕМА: Timeweb не запускает приложение

Timeweb распаковывает статику, но **НЕ запускает** `npm start`. Это означает, что:
1. Либо не указана команда запуска
2. Либо выбран неправильный тип приложения (статический вместо Node.js)
3. Либо порт настроен неправильно

---

## ✅ ЧЕКЛИСТ: Что проверить в панели Timeweb

### 1. Фреймворк (Framework)
**Где:** Settings → Build & Deploy → Framework

**Должно быть:**
- ✅ **Next.js** (или **Node.js**)
- ❌ НЕ должно быть: Static Site, HTML, или пусто

---

### 2. Команда запуска (Start Command / Runtime Command)
**Где:** Settings → Build & Deploy → Commands → **Start Command**

**Должно быть:**
```
npm start
```

**ВАЖНО:**
- ✅ Должно быть поле "Start Command" или "Runtime Command"
- ✅ Команда: `npm start` (без "run"!)
- ❌ НЕ должно быть пустым
- ❌ НЕ должно быть: `npm run start`

**Если такого поля НЕТ:**
- Timeweb может не поддерживать Node.js приложения в вашем тарифе
- Или нужно выбрать другой тип приложения

---

### 3. Порт (Port)
**Где:** Settings → Runtime → Port (или в настройках приложения)

**Должно быть:**
- ✅ **3000** (или порт, который указан в переменных окружения)

**Также проверь переменные окружения:**
- `PORT=3000` (если Timeweb требует)

---

### 4. Runtime логи (ОТДЕЛЬНО от Build логов!)
**Где:** Logs → **Runtime** (или Application Logs)

**Что искать:**
```
Ready on http://0.0.0.0:3000
```
или
```
started server on 0.0.0.0:3000
```

**Если таких строк НЕТ:**
- Приложение не запускается
- Команда запуска не указана или неправильная

---

### 5. Тип деплоя (Deploy Type)
**Где:** Settings → Build & Deploy

**Должно быть:**
- ✅ **Node.js Application** или **Next.js Application**
- ❌ НЕ должно быть: Static Site, HTML Site

---

## 🔧 РЕШЕНИЕ: Пошаговая инструкция

### Шаг 1: Проверь тип приложения
1. Открой **Settings → Build & Deploy**
2. Найди поле **"Framework"** или **"Application Type"**
3. Убедись, что выбрано **Next.js** или **Node.js**
4. Если там **Static Site** или **HTML** - измени на **Next.js**

### Шаг 2: Добавь команду запуска
1. В том же разделе найди **"Commands"** или **"Build & Deploy Commands"**
2. Должны быть 3 поля:
   - **Install Command:** `npm install`
   - **Build Command:** `npm run build`
   - **Start Command:** `npm start` ← **ЭТО КРИТИЧНО!**
3. Если поля "Start Command" **НЕТ** - это проблема!
   - Возможно, нужно выбрать другой тип приложения
   - Или Timeweb не поддерживает Node.js в вашем тарифе

### Шаг 3: Проверь порт
1. Открой **Settings → Runtime** (или **Environment**)
2. Найди поле **"Port"**
3. Установи: **3000**
4. Или добавь переменную окружения: `PORT=3000`

### Шаг 4: Пересобери приложение
1. Нажми **"Rebuild"** или **"Redeploy"**
2. Дождись завершения сборки
3. **СРАЗУ** открой **Runtime логи** (не Build логи!)
4. Ищи строки: `Ready on http://0.0.0.0:3000`

---

## 🚨 Если команды запуска НЕТ в панели

Если в панели Timeweb **нет поля "Start Command"**, это означает:

1. **Timeweb не распознает проект как Node.js приложение**
   - Решение: измени тип приложения на **Next.js** или **Node.js**

2. **Тариф не поддерживает Node.js приложения**
   - Решение: проверь тарифный план, возможно нужен более высокий тариф

3. **Timeweb использует Dockerfile (который мы удалили)**
   - Решение: создай минимальный Dockerfile (см. ниже)

---

## 📝 Альтернативное решение: Минимальный Dockerfile

Если Timeweb требует Dockerfile, создай файл `Dockerfile`:

```dockerfile
FROM node:20-slim

WORKDIR /app

# Копируем package файлы
COPY package*.json ./

# Устанавливаем зависимости (включая dev для build)
RUN npm ci --include=dev

# Копируем все файлы
COPY . .

# Собираем приложение
RUN npm run build

# Переменные окружения
ENV NODE_ENV=production
ENV PORT=3000

# Открываем порт
EXPOSE 3000

# Запускаем приложение
CMD ["npm", "start"]
```

**НО:** Сначала попробуй настроить команды в панели - это правильный способ!

---

## 📞 Что делать сейчас

1. **Открой панель Timeweb Cloud**
2. **Проверь все пункты из чеклиста выше**
3. **Пришли скриншот раздела "Commands" или "Build & Deploy"**
4. **Пришли Runtime логи** (последние 50 строк)

Без этой информации сложно точно сказать, в чем проблема.
