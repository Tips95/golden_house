# 🚀 Настройка автодеплоя на TimeWeb Cloud

## 📋 Информация о проекте

**Репозиторий:** https://github.com/Tips95/golden_house  
**Ветка:** `main`  
**Домен:** https://tips95-golden-house-dccb.twc1.net

---

## ⚙️ Настройки автодеплоя в панели TimeWeb Cloud

### 1. Repository Settings

```
Repository URL: https://github.com/Tips95/golden_house.git
Branch: main
```

### 2. Build Settings

**Install Command:**
```bash
npm install
```

**Build Command:**
```bash
npm run build
```

**Start Command:**
```bash
npm start
```

⚠️ **Важно:** Приложение использует режим статической генерации (`output: 'export'`):
- Next.js собирает все страницы в папку `out/` как статические HTML
- TimeWeb Cloud автоматически сервит эти файлы
- Не требуется отдельный Node.js сервер для запуска

### 3. Environment Variables (Переменные окружения)

Добавьте в панели TimeWeb Cloud:

```env
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_SITE_URL=https://tips95-golden-house-dccb.twc1.net
```

**Опционально (для аналитики):**
```env
NEXT_PUBLIC_YANDEX_METRIKA_ID=ваш_id
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=ваш_id
```

### 4. Deploy Settings

- **Node.js Version:** 20.x или выше
- **Package Manager:** npm
- **Root Directory:** `/` (корень репозитория)
- **Output Directory:** `out` (статический экспорт Next.js)

---

## 🔄 Процесс автодеплоя

После каждого `git push` в репозиторий:

1. ✅ TimeWeb Cloud получает webhook от GitHub
2. ✅ Выполняется `git pull` (получение последних изменений)
3. ✅ Выполняется `npm install` (установка зависимостей)
4. ✅ Выполняется `npm run build` (сборка Next.js проекта)
5. ✅ Выполняется `npm start` (запуск приложения на порту 3000)
6. ✅ Приложение становится доступно по домену

---

## 📝 Исправленные проблемы

### Проблема с портом (ИСПРАВЛЕНО ✅)

**Было:** 
```json
"start": "next start"
```

**Проблема:** TimeWeb передавал `PORT=3000`, а Next.js ожидал только `3000`

**Стало:**
```json
"start": "next start -p ${PORT:-3000}"
```

**Решение:** Команда теперь корректно извлекает значение порта из переменной окружения

---

## 🧪 Проверка после деплоя

После успешного деплоя проверьте:

### 1. Доступность сайта
```
https://tips95-golden-house-dccb.twc1.net
```

### 2. Основные страницы
- ✅ Главная: `/`
- ✅ Услуги: `/services`
- ✅ Конкретная услуга: `/services/mekhanizirovannaya-shtukaturka`
- ✅ Портфолио: `/portfolio`
- ✅ Контакты: `/contacts`
- ✅ О компании: `/about`
- ✅ Блог: `/blog`

### 3. Функциональность
- ✅ WhatsApp кнопки работают
- ✅ Калькуляторы работают
- ✅ Навигация работает
- ✅ Мобильная версия адаптивна

---

## 📊 Логи и мониторинг

### Просмотр логов в панели TimeWeb

1. Откройте панель управления TimeWeb Cloud
2. Перейдите в раздел вашего приложения
3. Выберите вкладку "Логи" или "Logs"
4. Просмотрите логи сборки и запуска

### Типичные логи при успешном деплое

```
✓ Compiled successfully
✓ Generating static pages (24/24)
Route (app)                              Size     First Load JS
┌ ○ /                                    8.5 kB   154 kB
├ ○ /services                            175 B    96.1 kB
└ ● /services/[slug]                     9.85 kB  133 kB
```

---

## 🐛 Troubleshooting

### Приложение не запускается

**Проверьте переменные окружения:**
- `NODE_ENV=production` установлен
- `PORT=3000` установлен
- `NEXT_PUBLIC_SITE_URL` установлен с правильным доменом

**Проверьте логи:**
- Нет ошибок сборки (`npm run build`)
- Нет ошибок установки зависимостей (`npm install`)

### Ошибка "Invalid port"

**Решение:** Убедитесь, что в `package.json` команда `start` содержит:
```json
"start": "next start -p ${PORT:-3000}"
```

### Сайт показывает 404

**Возможные причины:**
- Неправильный путь к файлам
- Не завершилась сборка
- Не все статические страницы сгенерированы

**Решение:**
- Проверьте логи сборки
- Убедитесь, что `npm run build` прошел успешно
- Проверьте, что все файлы из `.next` директории доступны

### Стили не загружаются

**Решение:**
- Убедитесь, что `NEXT_PUBLIC_SITE_URL` установлен правильно
- Проверьте, что CDN или статические файлы доступны

---

## 🔄 Обновление сайта

### Локальная разработка

```bash
# Внесите изменения в код
# ...

# Проверьте локально
npm run build
npm run start

# Закоммитьте изменения
git add .
git commit -m "Описание изменений"

# Отправьте в GitHub
git push origin main
```

### Автоматический деплой

После `git push` TimeWeb Cloud автоматически:
1. Обнаружит изменения через webhook
2. Выполнит новую сборку
3. Перезапустит приложение

**Время деплоя:** 2-5 минут

---

## 📈 Производительность

### Оптимизации уже настроены:

- ✅ **Next.js 14** - App Router, SSR/SSG
- ✅ **Статическая генерация** - 15 страниц услуг
- ✅ **Оптимизация изображений** - WebP/AVIF
- ✅ **Компрессия** - Gzip включен в Next.js
- ✅ **Минификация** - Автоматическая в production

### Ожидаемые метрики:

- ⚡ **Time to First Byte:** < 500ms
- ⚡ **First Contentful Paint:** < 1.5s
- ⚡ **Largest Contentful Paint:** < 2.5s
- ⚡ **Lighthouse Score:** > 90

---

## 🔐 Безопасность

### Что уже настроено:

- ✅ Переменные окружения защищены
- ✅ `.env` файлы не в репозитории (`.gitignore`)
- ✅ HTTPS автоматически от TimeWeb Cloud
- ✅ Security headers в Next.js
- ✅ `poweredByHeader: false` в конфигурации

---

## 📞 Поддержка

### Контакты компании
- **Email:** Golden.House.Services@mail.ru
- **WhatsApp:** +7 (928) 195-88-85

### TimeWeb Cloud Support
- **Документация:** https://timeweb.cloud/help/
- **Тикеты:** В личном кабинете

---

## ✅ Чеклист готовности

- [x] Репозиторий настроен на GitHub
- [x] Все файлы запушены в `main` ветку
- [x] `package.json` содержит исправленную команду `start`
- [x] `.env.production` содержит правильный домен
- [x] Переменные окружения настроены в панели TimeWeb
- [x] Автодеплой настроен в панели TimeWeb
- [x] Проект собирается без ошибок (`npm run build`)
- [x] Все TypeScript ошибки исправлены

---

## 🎉 Готово!

Ваш проект настроен для автодеплоя на TimeWeb Cloud!

**Текущий статус:**
- ✅ Репозиторий: https://github.com/Tips95/golden_house
- ✅ Домен: https://tips95-golden-house-dccb.twc1.net
- ✅ Автодеплой: Настроен

**При каждом `git push` проект автоматически обновится на сервере!** 🚀
