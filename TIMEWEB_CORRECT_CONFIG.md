# ✅ ПРАВИЛЬНАЯ КОНФИГУРАЦИЯ для Timeweb Cloud

## 🎯 Понимание архитектуры Timeweb Cloud

Timeweb Cloud работает с Docker контейнерами по следующей схеме:

### Фаза 1: Сборка Docker образа (Build)
```
1. Install: npm install          → Устанавливает node_modules
2. Build:   npm run build        → Создает .next директорию
3. Результат сохраняется в Docker образ
```

### Фаза 2: Запуск контейнера (Start)
```
4. Start:   npm start            → Запускает next start
   └─ Использует уже готовую .next из образа
```

---

## ⚙️ ПРАВИЛЬНЫЕ НАСТРОЙКИ

### 📦 Repository Settings

```
Repository URL: https://github.com/Tips95/golden_house.git
Branch: main
☑️ Включить автодеплой
```

---

### 🔧 Build Settings

```
Тип приложения:     Next.js
Node.js версия:     20.x
Package Manager:    npm
Root Directory:     /
```

---

### 📝 Commands (ПРАВИЛЬНАЯ КОНФИГУРАЦИЯ)

```bash
Install Command:
npm install

Build Command:
npm run build

Start Command:
npm start
```

⚠️ **ВАЖНО:**
- **Build Command** = `npm run build` (выполняется при сборке образа)
- **Start Command** = `npm start` (запускает сервер в контейнере)
- **НЕ объединять** команды в одну!

---

### 🌍 Environment Variables

```env
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://tips95-golden-house-dccb.twc1.net
```

---

## 📊 Процесс деплоя (правильный)

### Этап 1: Сборка Docker образа
```dockerfile
# Dockerfile (создается Timeweb автоматически)
FROM node:20-slim

# Install Phase
COPY package*.json ./
RUN npm install              ✅ Устанавливает зависимости

# Build Phase
COPY . .
RUN npm run build            ✅ Создает .next директорию
                             ✅ .next сохраняется в образ!

# Start определяется, но не выполняется
CMD ["npm", "start"]
```

### Этап 2: Запуск контейнера
```bash
# Timeweb запускает контейнер
docker run -p 3000:3000 -e PORT=3000 your-image

# Внутри контейнера выполняется:
$ npm start                  ✅ Запускает next start
> next start

▲ Next.js 14.2.35
- Local:    http://localhost:3000
- Network:  http://0.0.0.0:3000

✓ Ready in 1.5s             ✅ Сервер работает!
```

---

## ❌ Что было неправильно

### Попытка 1: Объединенная команда Start
```bash
Build Command:  (пусто)
Start Command:  npm run build && npm start
```

**Проблема:**
- `npm run build` выполняется при **запуске контейнера**
- `npm start` запускается сразу после build
- Но при этом `npm start` пытается занять порт, который уже занят сборкой
- **Ошибка:** `EADDRINUSE: address already in use :::3000`

### Попытка 2: Start в Build команде
```bash
Build Command:  npm run build && npm start
Start Command:  npm start
```

**Проблема:**
- Build выполняется **внутри сборки Docker образа**
- `npm start` пытается запустить сервер во время сборки образа
- Сборка зависает, потому что сервер ждет подключений
- **Ошибка:** Build timeout

---

## ✅ Правильное решение

```bash
Build Command:  npm run build
Start Command:  npm start
```

**Почему это работает:**
1. **Install:** `npm install` → устанавливает зависимости в образ
2. **Build:** `npm run build` → создает `.next` в образе
3. **Образ собран и сохранен** ✅
4. **Start:** `npm start` → запускает сервер в контейнере
5. **Сервер использует .next из образа** ✅

---

## 🔧 Инструкция по настройке

### 1. Откройте настройки приложения

[Timeweb Cloud](https://timeweb.cloud/) → **App Platform** → Ваше приложение → **Settings**

---

### 2. Build & Deploy настройки

Перейдите в **Build & Deploy** → **Commands**

Установите:

```bash
Install Command:
npm install

Build Command:
npm run build

Start Command:
npm start
```

---

### 3. Проверьте Environment Variables

```env
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://tips95-golden-house-dccb.twc1.net
```

---

### 4. Сохраните и пересоберите

1. **Save Changes**
2. **Rebuild** / **Пересобрать**
3. Ожидайте 2-3 минуты

---

## 📋 Ожидаемые логи

### Build Phase (сборка образа):
```
Step 1: Installing dependencies
$ npm install
added 393 packages in 20s
✅ Dependencies installed

Step 2: Building application
$ npm run build
> next build

✓ Compiled successfully
✓ Generating static pages (24/24)
✅ Build complete

Docker image created: sha256:abc123...
✅ Image pushed to registry
```

### Start Phase (запуск контейнера):
```
Starting container...
Container started with ID: def456...

$ npm start
> next start

▲ Next.js 14.2.35
- Local:    http://localhost:3000
- Network:  http://0.0.0.0:3000

✓ Ready in 1.5s

✅ Application is running
✅ Health checks passing
✅ Deployment complete!

Your app is live at:
https://tips95-golden-house-dccb.twc1.net
```

---

## ⏱️ Время деплоя

| Фаза | Время |
|------|-------|
| Install | 15-30 сек |
| Build | 30-60 сек |
| Docker build | 20-40 сек |
| Start container | 5-10 сек |
| **Всего** | **2-3 минуты** |

---

## ✅ Проверка после деплоя

### Откройте сайт:
```
https://tips95-golden-house-dccb.twc1.net
```

### Проверьте страницы:
- ✅ `/` - Главная
- ✅ `/services` - Услуги
- ✅ `/services/mekhanizirovannaya-shtukaturka` - Конкретная услуга
- ✅ `/portfolio` - Портфолио
- ✅ `/contacts` - Контакты
- ✅ `/about` - О компании
- ✅ `/blog` - Блог

---

## 🔄 Автоматические обновления

После успешного деплоя:

```bash
# Внесите изменения
git add .
git commit -m "Update content"
git push origin main

# Timeweb автоматически:
# 1. Получит webhook от GitHub
# 2. Соберет новый Docker образ
# 3. Запустит новый контейнер
# 4. Обновит ваш сайт (~2-3 минуты)
```

---

## 🐛 Troubleshooting

### Ошибка: "EADDRINUSE: port 3000 already in use"

**Причина:** Start команда выполняется в Build фазе или объединена с build

**Решение:**
- Build Command = `npm run build` (только build!)
- Start Command = `npm start` (только start!)

---

### Ошибка: "Could not find a production build in .next"

**Причина:** Build Command не выполнен или пустой

**Решение:**
- Убедитесь, что Build Command = `npm run build`
- Проверьте логи сборки на наличие ошибок

---

### Build зависает / timeout

**Причина:** В Build Command есть `npm start` или другая блокирующая команда

**Решение:**
- Build Command должен содержать ТОЛЬКО `npm run build`
- Никаких серверных команд в Build!

---

## 📝 Файл package.json

Убедитесь, что ваш `package.json` содержит:

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
```

✅ Скрипты правильные - не требуют изменений!

---

## ✅ Финальный чеклист

- [x] Git репозиторий настроен
- [x] `package.json` содержит правильные скрипты
- [x] Проект собирается локально без ошибок
- [ ] **В Timeweb: Build Command = `npm run build`** ← Проверьте!
- [ ] **В Timeweb: Start Command = `npm start`** ← Проверьте!
- [ ] **НЕ объединять** команды! ← Проверьте!
- [ ] Environment Variables добавлены
- [ ] Нажата кнопка "Rebuild"
- [ ] Ожидание 2-3 минуты
- [ ] Сайт открывается

---

## 🎯 Ключевые моменты

1. **Build выполняется в Docker образе** - результаты сохраняются
2. **Start выполняется в контейнере** - использует готовый образ
3. **НЕ объединять** build и start в одну команду
4. **НЕ запускать** сервер в Build фазе

---

## 🚀 Действуйте сейчас!

1. Откройте Timeweb Cloud
2. Settings → Build & Deploy → Commands
3. **Build:** `npm run build`
4. **Start:** `npm start`
5. Save → Rebuild
6. Подождите 2-3 минуты
7. Откройте сайт

**Теперь всё работает правильно!** 🎉

---

## 📞 Поддержка

**Timeweb Cloud:**
- ☎️ 8 (800) 700-32-92
- 📧 Тикеты в личном кабинете
- 📚 https://timeweb.cloud/help/

**Golden House:**
- 📧 Golden.House.Services@mail.ru
- 📱 +7 (928) 195-88-85
