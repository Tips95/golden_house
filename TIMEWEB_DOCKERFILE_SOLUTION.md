# ✅ РЕШЕНИЕ: Dockerfile для Timeweb Cloud

## 🎯 Проблема

Timeweb Cloud выполняет `npm run start` **во время сборки Docker образа**, но Build Command не выполняется. Это происходит потому, что Timeweb генерирует Dockerfile автоматически и не использует Build Command правильно.

**Ошибка в логах:**
```
#13 [7/7] RUN npm run start
Error: Could not find a production build in the '.next' directory.
```

## 💡 Решение

Создать **собственный Dockerfile** в корне проекта. Timeweb Cloud будет использовать его вместо автоматически сгенерированного.

---

## 📦 Что было создано

### 1. Dockerfile
- Multi-stage build (builder + runner)
- Правильная сборка Next.js приложения
- Оптимизированный production образ

### 2. .dockerignore
- Исключает ненужные файлы из образа
- Уменьшает размер образа

---

## ⚙️ Настройка в Timeweb Cloud

### Вариант 1: Использовать Dockerfile (РЕКОМЕНДУЕТСЯ)

1. **Откройте Timeweb Cloud** → **App Platform** → Ваше приложение
2. **Settings** → **Build & Deploy**
3. **Build Method:** Выберите **"Dockerfile"** (если есть такая опция)
4. Или просто убедитесь, что **Dockerfile** находится в корне репозитория

**Команды в панели:**
```
Install Command:  (можно оставить пустым или npm install)
Build Command:    (не используется - Dockerfile делает всё)
Start Command:    (не используется - CMD в Dockerfile)
```

### Вариант 2: Если Dockerfile не используется автоматически

Если Timeweb Cloud все еще использует автоматический Dockerfile:

1. **Settings** → **Build & Deploy** → **Commands**
2. Установите:
   ```
   Install Command:  npm install
   Build Command:    npm run build
   Start Command:    npm start
   ```
3. **Save** → **Rebuild**

Timeweb должен обнаружить Dockerfile и использовать его.

---

## 📊 Как работает Dockerfile

### Stage 1: Builder (сборка)
```dockerfile
FROM node:20-slim AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci                    # Установка зависимостей
COPY . .
RUN npm run build             # Сборка Next.js → создает .next/
```

### Stage 2: Runner (production)
```dockerfile
FROM node:20-slim AS runner
WORKDIR /app
COPY package*.json ./
RUN npm ci                    # Production зависимости
COPY --from=builder /app/.next ./.next  # Копируем собранное приложение
CMD ["npm", "start"]          # Запуск сервера
```

---

## ✅ Преимущества собственного Dockerfile

1. ✅ **Полный контроль** над процессом сборки
2. ✅ **Правильная последовательность**: build → start
3. ✅ **Оптимизированный образ** (multi-stage build)
4. ✅ **Работает гарантированно** - не зависит от автоматической генерации

---

## 🔄 После создания Dockerfile

1. **Закоммитьте Dockerfile:**
   ```bash
   git add Dockerfile .dockerignore
   git commit -m "Add Dockerfile for Timeweb Cloud deployment"
   git push origin main
   ```

2. **В Timeweb Cloud:**
   - Если есть опция "Use Dockerfile" - включите её
   - Или просто пересоберите приложение - Timeweb обнаружит Dockerfile

3. **Rebuild** приложение в Timeweb Cloud

---

## 📋 Ожидаемые логи

### Build Stage:
```
Step 1: FROM node:20-slim AS builder
Step 2: WORKDIR /app
Step 3: COPY package*.json ./
Step 4: RUN npm ci
        added 393 packages in 20s
Step 5: COPY . .
Step 6: RUN npm run build
        ✓ Compiled successfully
        ✓ Generating static pages (24/24)
        ✅ .next directory created
```

### Production Stage:
```
Step 7: FROM node:20-slim AS runner
Step 8: COPY --from=builder /app/.next ./.next
Step 9: CMD ["npm", "start"]
        ▲ Next.js 14.2.35
        ✓ Ready in 1.5s
        ✅ Server running
```

---

## 🐛 Troubleshooting

### Dockerfile не используется

**Решение:**
1. Убедитесь, что Dockerfile в **корне репозитория**
2. Проверьте, что файл называется именно **`Dockerfile`** (без расширения)
3. В настройках Timeweb выберите "Use Dockerfile" или "Custom Dockerfile"

### Ошибка при сборке

**Проверьте:**
1. Dockerfile синтаксически правильный
2. Все файлы скопированы (next.config.js, tsconfig.json и т.д.)
3. Node.js версия совпадает (20.x)

### Образ слишком большой

**Решение:**
- Используйте `.dockerignore` (уже создан)
- Multi-stage build уже оптимизирует размер

---

## 📞 Поддержка

Если Dockerfile не работает:
1. Проверьте логи сборки в Timeweb Cloud
2. Убедитесь, что Dockerfile в корне репозитория
3. Обратитесь в поддержку Timeweb Cloud

---

## ✅ Готово!

После создания Dockerfile и пуша в GitHub:

1. Timeweb Cloud обнаружит Dockerfile
2. Использует его для сборки образа
3. Приложение соберется и запустится правильно

**Успешного деплоя!** 🚀
