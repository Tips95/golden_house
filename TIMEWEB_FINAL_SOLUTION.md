# ✅ Финальное решение для Timeweb Cloud

## 🎯 Проблема

Timeweb Cloud не сохраняет результаты сборки (директорию `.next`) между фазами Build и Start. При попытке запустить `npm start` после отдельной фазы Build получаем ошибку:

```
Error: Could not find a production build in the '.next' directory.
```

## 💡 Решение

Объединить команды Build и Start в одну команду Start.

---

## ⚙️ Правильная настройка в Timeweb Cloud

### 1. Repository Settings

```
Repository URL: https://github.com/Tips95/golden_house.git
Branch: main
☑️ Включить автодеплой
```

---

### 2. Build Settings

```
Тип приложения:     Next.js
Node.js версия:     20.x или выше
Package Manager:    npm
Root Directory:     /
```

---

### 3. Commands (ВАЖНО!)

```bash
Install Command:   npm install

Build Command:     (ОСТАВИТЬ ПУСТЫМ или УДАЛИТЬ)

Start Command:     npm run build && npm start
```

⚠️ **КРИТИЧНО:**
- Поле **Build Command** должно быть **ПУСТЫМ**!
- Вся логика в команде **Start Command**: `npm run build && npm start`

---

### 4. Environment Variables

```env
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://tips95-golden-house-dccb.twc1.net
```

---

## 📊 Как это работает

### Последовательность выполнения:

```
1. Install Phase:
   └─ npm install                    ✅ (устанавливает зависимости)

2. Start Phase:
   ├─ npm run build                  ✅ (собирает проект → создает .next/)
   │  └─ Генерация 24 страниц
   └─ npm start                      ✅ (запускает next start)
      └─ Next.js сервер запущен на порту 3000

3. Deploy Complete!                  ✅
```

---

## 🔄 Почему не работало раздельно

**Попытка 1: Build и Start отдельно**
```
Build Command:  npm run build
Start Command:  npm start
```

❌ **Результат:**
- Build Phase создает `.next/` → ✅
- Но Timeweb запускает Start в **новом контейнере** → ❌
- `.next/` директории нет → Error!

**Попытка 2: Только Start без Build**
```
Build Command:  (пусто)
Start Command:  npm start
```

❌ **Результат:**
- Сразу запускается `next start` → ❌
- `.next/` не существует → Error!

**Решение: Объединенная команда Start** ✅
```
Build Command:  (пусто)
Start Command:  npm run build && npm start
```

✅ **Результат:**
- В Start Phase выполняется сначала build → `.next/` создается → ✅
- Сразу после этого запускается start → `.next/` доступна → ✅
- Сервер запускается успешно! → ✅

---

## 📝 Пошаговая инструкция

### Если приложение уже создано:

1. Откройте ваше приложение в [Timeweb Cloud](https://timeweb.cloud/)
2. **Settings** → **Build & Deploy**
3. Измените команды на:
   ```
   Install Command:  npm install
   Build Command:    (удалите всё, оставьте пустым)
   Start Command:    npm run build && npm start
   ```
4. **Save Changes**
5. **Rebuild** → Запустить новый деплой

---

### Если создаете новое приложение:

1. **App Platform** → **Создать приложение**
2. **Из Git-репозитория**
3. Подключите: `https://github.com/Tips95/golden_house.git`
4. **Build Settings:**
   ```
   Install:  npm install
   Build:    (оставить пустым)
   Start:    npm run build && npm start
   ```
5. Добавьте переменные окружения (NODE_ENV, NEXT_PUBLIC_SITE_URL)
6. **Создать и развернуть**

---

## ⏱️ Время деплоя

- **Install:** ~15-30 сек
- **Build (в Start):** ~30-60 сек
- **Start сервера:** ~5-10 сек
- **Всего:** ~2-3 минуты

---

## ✅ Ожидаемые логи

```
=== Install Phase ===
npm install
added 393 packages in 20s
✅ Install complete

=== Start Phase ===
npm run build && npm start

> next build
✓ Compiled successfully
✓ Generating static pages (24/24)
✅ Build complete

> next start
▲ Next.js 14.2.35
- Local:    http://localhost:3000
- Network:  http://0.0.0.0:3000

✓ Ready in 1.5s
✅ Server started

=== Deploy Complete ===
Your app is running at:
https://tips95-golden-house-dccb.twc1.net
```

---

## 🎉 После успешного деплоя

Ваш сайт будет доступен по адресу:
```
https://tips95-golden-house-dccb.twc1.net
```

**Автоматические обновления:**
- При каждом `git push` → автоматический деплой
- Время обновления: ~2-3 минуты
- HTTPS работает автоматически

---

## 🐛 Troubleshooting

### Ошибка: "Could not find a production build"

**Причина:** Build Command не пустой, или Start Command не содержит `npm run build`

**Решение:**
1. Убедитесь, что Build Command **ПУСТОЙ**
2. Start Command должен быть: `npm run build && npm start`

---

### Ошибка: "Port 3000 is already in use"

**Причина:** Переменная PORT не передается

**Решение:**
- Timeweb автоматически передает `PORT`
- Если проблема не исчезает, добавьте в переменные окружения: `PORT=3000`

---

### Деплой занимает слишком много времени

**Нормально:** 
- Первый деплой: 3-5 минут
- Последующие: 2-3 минуты

**Если дольше:**
- Проверьте логи на наличие ошибок
- Убедитесь, что нет зависших процессов

---

## 📞 Поддержка

**Timeweb Cloud:**
- 📧 Тикеты в личном кабинете
- ☎️ 8 (800) 700-32-92
- 📚 https://timeweb.cloud/help/

**Golden House:**
- 📧 Golden.House.Services@mail.ru
- 📱 +7 (928) 195-88-85

---

## ✅ Чеклист готовности

- [x] Git репозиторий настроен
- [x] Изменения запушены в GitHub
- [x] `package.json` содержит `"start": "next start"`
- [x] Проект собирается локально без ошибок
- [ ] **В Timeweb Cloud Build Command = ПУСТОЙ** ← Проверьте!
- [ ] **В Timeweb Cloud Start Command = `npm run build && npm start`** ← Проверьте!
- [ ] Переменные окружения добавлены
- [ ] Запущен деплой
- [ ] Сайт открывается по домену

---

## 🚀 Готово к деплою!

Настройте команды согласно этой инструкции и запустите деплой в Timeweb Cloud!
