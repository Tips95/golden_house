# 🚀 Быстрая установка на TimeWeb Cloud

Это упрощенная инструкция с готовыми скриптами для деплоя проекта на TimeWeb Cloud.

## 📋 Предварительные требования

1. Сервер на TimeWeb Cloud (Ubuntu 22.04 LTS)
2. Доменное имя (привязанное к серверу)
3. SSH доступ к серверу

---

## 🎯 Быстрый старт (5 шагов)

### Шаг 1: Подключитесь к серверу

```bash
ssh root@ВАШ_IP_АДРЕС
```

### Шаг 2: Загрузите и запустите скрипт настройки сервера

```bash
# Скачайте проект
cd /tmp
git clone YOUR_REPO_URL goldenhouse-temp
cd goldenhouse-temp

# Запустите скрипт настройки сервера
bash server-setup.sh
```

Этот скрипт автоматически установит:
- ✅ Node.js 20.x
- ✅ PM2 (менеджер процессов)
- ✅ Nginx (веб-сервер)
- ✅ Git
- ✅ Certbot (для SSL)
- ✅ Настроит firewall

### Шаг 3: Клонируйте проект

```bash
cd /var/www
git clone YOUR_REPO_URL goldenhouse-services
cd goldenhouse-services
```

### Шаг 4: Настройте переменные окружения

```bash
# Скопируйте пример конфигурации
cp .env.example .env.production

# Отредактируйте файл
nano .env.production
```

Измените следующие значения:

```env
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_SITE_URL=https://ваш-домен.ru
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 5: Установите и запустите приложение

```bash
# Установите зависимости
npm install

# Соберите проект
npm run build

# Запустите с PM2
pm2 start ecosystem.config.js

# Настройте автозапуск
pm2 startup
pm2 save
```

---

## 🌐 Настройка Nginx и SSL

### Настройте Nginx

```bash
# Скопируйте конфигурацию
cp nginx.conf /etc/nginx/sites-available/goldenhouse-services

# Откройте для редактирования
nano /etc/nginx/sites-available/goldenhouse-services
```

**Замените** `yourdomain.ru` на ваш реальный домен во всех местах.

```bash
# Активируйте конфигурацию
ln -s /etc/nginx/sites-available/goldenhouse-services /etc/nginx/sites-enabled/

# Удалите дефолтную конфигурацию
rm /etc/nginx/sites-enabled/default

# Проверьте конфигурацию
nginx -t

# Перезапустите Nginx
systemctl reload nginx
```

### Установите SSL сертификат

```bash
# Замените yourdomain.ru на ваш домен
certbot --nginx -d yourdomain.ru -d www.yourdomain.ru
```

Certbot автоматически:
- ✅ Получит бесплатный SSL сертификат
- ✅ Настроит HTTPS
- ✅ Настроит автообновление сертификата

---

## ✅ Проверка

Откройте в браузере: `https://ваш-домен.ru`

Проверьте работу:
- ✅ Главная страница
- ✅ Страницы услуг
- ✅ Портфолио
- ✅ Контакты
- ✅ WhatsApp кнопки

---

## 🔄 Обновление сайта

Когда вы внесли изменения в код и хотите обновить сайт на сервере:

```bash
# Подключитесь к серверу
ssh root@ВАШ_IP

# Запустите скрипт деплоя
cd /var/www/goldenhouse-services
bash deploy.sh
```

Скрипт `deploy.sh` автоматически:
1. Получит последние изменения из Git
2. Установит зависимости
3. Соберет проект
4. Перезапустит приложение

---

## 📊 Полезные команды

### Управление PM2

```bash
# Статус приложения
pm2 status

# Просмотр логов (в реальном времени)
pm2 logs goldenhouse-services

# Перезапуск
pm2 restart goldenhouse-services

# Остановка
pm2 stop goldenhouse-services

# Мониторинг ресурсов
pm2 monit
```

### Управление Nginx

```bash
# Проверка конфигурации
nginx -t

# Перезагрузка конфигурации
systemctl reload nginx

# Перезапуск
systemctl restart nginx

# Статус
systemctl status nginx

# Просмотр логов
tail -f /var/log/nginx/goldenhouse-access.log
tail -f /var/log/nginx/goldenhouse-error.log
```

### SSL сертификаты

```bash
# Проверка статуса сертификата
certbot certificates

# Обновление сертификата вручную
certbot renew

# Тестовое обновление
certbot renew --dry-run
```

---

## 🐛 Решение проблем

### Приложение не запускается

```bash
# Проверьте логи PM2
pm2 logs goldenhouse-services --lines 50

# Проверьте, что порт 3000 свободен
netstat -tulpn | grep 3000

# Попробуйте запустить вручную
cd /var/www/goldenhouse-services
npm run start
```

### Nginx возвращает 502 Bad Gateway

```bash
# Убедитесь, что приложение запущено
pm2 status

# Проверьте логи Nginx
tail -f /var/log/nginx/goldenhouse-error.log

# Перезапустите приложение
pm2 restart goldenhouse-services
```

### SSL не работает

```bash
# Проверьте конфигурацию Nginx
nginx -t

# Убедитесь, что сертификат получен
certbot certificates

# Обновите сертификат
certbot renew --force-renewal

# Перезапустите Nginx
systemctl restart nginx
```

---

## 📁 Структура проекта на сервере

```
/var/www/goldenhouse-services/
├── .next/                    # Собранное приложение
├── src/                      # Исходный код
├── public/                   # Статические файлы
├── node_modules/             # Зависимости
├── logs/                     # Логи PM2
├── .env.production           # Переменные окружения
├── ecosystem.config.js       # Конфигурация PM2
├── nginx.conf                # Конфигурация Nginx
├── deploy.sh                 # Скрипт деплоя
└── package.json              # Зависимости проекта
```

---

## 🔒 Безопасность

### Базовые настройки безопасности

```bash
# Создайте нового пользователя (не root)
adduser deployer
usermod -aG sudo deployer

# Запретите вход под root через SSH
nano /etc/ssh/sshd_config
# Найдите и измените: PermitRootLogin no
systemctl restart sshd

# Настройте firewall
ufw allow 22
ufw allow 80
ufw allow 443
ufw enable
```

---

## 📈 Мониторинг и аналитика

### Подключите Яндекс.Метрику

1. Получите ID счетчика в метрике
2. Добавьте в `.env.production`:

```env
NEXT_PUBLIC_YANDEX_METRIKA_ID=ваш_id
```

3. Добавьте код метрики в `src/app/layout.tsx`

### Настройте мониторинг сервера

```bash
# Установите htop для мониторинга
apt install htop

# Запустите
htop
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `pm2 logs`
2. Проверьте статус: `pm2 status`
3. Проверьте Nginx: `systemctl status nginx`
4. Напишите на: Golden.House.Services@mail.ru

---

## ✅ Чеклист после установки

- [ ] Сервер настроен (Node.js, PM2, Nginx установлены)
- [ ] Проект клонирован в `/var/www/goldenhouse-services`
- [ ] Переменные окружения настроены в `.env.production`
- [ ] Проект собран (`npm run build`)
- [ ] PM2 запущен и настроен автозапуск
- [ ] Nginx настроен с вашим доменом
- [ ] SSL сертификат установлен
- [ ] DNS настроен (A-запись на IP сервера)
- [ ] Сайт открывается по HTTPS
- [ ] WhatsApp кнопки работают
- [ ] Все страницы загружаются корректно

---

**🎉 Готово! Ваш сайт работает на TimeWeb Cloud!**

Для обновлений используйте: `bash deploy.sh`
