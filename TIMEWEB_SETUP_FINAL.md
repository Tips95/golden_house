# 🚀 Финальная настройка для TimeWeb Cloud

## 📋 Что было сделано

Проект настроен для **статической генерации** (Static Site Generation) согласно требованиям TimeWeb Cloud.

### ✅ Изменения в проекте:

1. **Next.js в режиме статического экспорта** (`output: 'export'`)
   - Все страницы генерируются как статические HTML файлы
   - Папка сборки: `out/`

2. **Добавлен `serve` как зависимость**
   - Используется для запуска статического веб-сервера
   - Слушает на `0.0.0.0` (доступен извне контейнера)
   - Использует переменную окружения `$PORT` от TimeWeb

3. **Команда start исправлена**
   ```json
   "start": "serve out -n -l tcp://0.0.0.0:$PORT"
   ```

---

## ⚙️ НАСТРОЙКА В ПАНЕЛИ TIMEWEB CLOUD

Откройте **App Platform** → Ваше приложение → **Настройки**

### 1️⃣ Repository Settings

```
Repository URL: https://github.com/Tips95/golden_house.git
Branch: main
```

✅ Убедитесь, что включен **автодеплой** (автоматическая сборка при коммите)

---

### 2️⃣ Build Settings

#### Тип приложения:
```
Next.js
```

#### Node.js Version:
```
20.x (или выше)
```

#### Package Manager:
```
npm
```

#### Install Command:
```
npm install
```

#### Build Command:
```
npm run build
```

#### Start Command:
```
npm start
```

#### 📁 Директория сборки (Output Directory):
```
out
```

☝️ **ЭТО КРИТИЧНО!** Next.js с `output: 'export'` создает папку `out/`, а не `.next`

---

### 3️⃣ Environment Variables (Переменные окружения)

Добавьте следующие переменные:

| Переменная | Значение |
|------------|----------|
| `NODE_ENV` | `production` |
| `NEXT_PUBLIC_SITE_URL` | `https://tips95-golden-house-dccb.twc1.net` |

**Опционально (для аналитики):**
| Переменная | Значение |
|------------|----------|
| `NEXT_PUBLIC_YANDEX_METRIKA_ID` | ваш_id |
| `NEXT_PUBLIC_GOOGLE_ANALYTICS_ID` | ваш_id |

⚠️ **ВАЖНО:** Переменная `PORT` **НЕ НУЖНА** - TimeWeb автоматически передаст её в контейнер!

---

### 4️⃣ Region Settings

Выберите регион размещения:
- Рекомендуется: **Москва** (для России)

---

## 🔄 Процесс деплоя

После сохранения настроек и нажатия **"Пересобрать"** / **"Rebuild"**:

1. ✅ TimeWeb выполнит `git clone` и `git pull`
2. ✅ Установит зависимости: `npm install`
3. ✅ Соберет проект: `npm run build`
   - Next.js создаст 24 статические HTML страницы в `out/`
4. ✅ Запустит сервер: `npm start`
   - `serve` запустится на порту `$PORT` (передается TimeWeb)
   - Будет слушать на `0.0.0.0` (доступен извне)
5. ✅ TimeWeb настроит reverse proxy с вашего домена на контейнер
6. ✅ Сайт будет доступен по адресу: `https://tips95-golden-house-dccb.twc1.net`

---

## 📊 Ожидаемые логи при успешном деплое

### Фаза установки:
```
npm install
added 449 packages in 15s
```

### Фаза сборки:
```
npm run build
✓ Compiled successfully
✓ Generating static pages (24/24)
Route (app)                              Size     First Load JS
┌ ○ /                                    8.5 kB   154 kB
├ ○ /services                            175 B    96.2 kB
└ ● /services/[slug]                     9.85 kB  133 kB
```

### Фаза запуска:
```
npm start
> serve out -n -l tcp://0.0.0.0:$PORT

INFO  Accepting connections at tcp://0.0.0.0:3000
```

☝️ Если вы видите `tcp://0.0.0.0:3000` - **это хорошо!** Сервер слушает на всех интерфейсах.

---

## ✅ Проверка после деплоя

### 1️⃣ Откройте сайт:
```
https://tips95-golden-house-dccb.twc1.net
```

### 2️⃣ Проверьте основные страницы:

- ✅ Главная: [/](https://tips95-golden-house-dccb.twc1.net/)
- ✅ Услуги: [/services](https://tips95-golden-house-dccb.twc1.net/services)
- ✅ Конкретная услуга: [/services/mekhanizirovannaya-shtukaturka](https://tips95-golden-house-dccb.twc1.net/services/mekhanizirovannaya-shtukaturka)
- ✅ Портфолио: [/portfolio](https://tips95-golden-house-dccb.twc1.net/portfolio)
- ✅ Контакты: [/contacts](https://tips95-golden-house-dccb.twc1.net/contacts)
- ✅ О компании: [/about](https://tips95-golden-house-dccb.twc1.net/about)
- ✅ Блог: [/blog](https://tips95-golden-house-dccb.twc1.net/blog)

### 3️⃣ Проверьте функциональность:

- ✅ WhatsApp кнопки работают
- ✅ Калькуляторы открываются
- ✅ Навигация работает
- ✅ Мобильная версия адаптивна

---

## 🐛 Troubleshooting

### ❌ Сайт не открывается (404)

**Причины:**
1. Директория сборки указана неправильно
2. Build завершился с ошибками

**Решение:**
- Проверьте, что в настройках указано: `Output Directory: out`
- Посмотрите логи сборки - там должно быть `✓ Generating static pages (24/24)`

---

### ❌ В логах: "Cannot find module 'serve'"

**Причина:** Зависимость не установилась

**Решение:**
- Убедитесь, что в `package.json` есть `"serve": "^14.2.5"` в `dependencies`
- Пересоберите приложение (Rebuild)

---

### ❌ В логах: "Accepting connections at localhost:3000"

**Причина:** Serve слушает только на localhost, а не на 0.0.0.0

**Решение:**
- Проверьте команду `start` в `package.json`:
  ```json
  "start": "serve out -n -l tcp://0.0.0.0:$PORT"
  ```
- Если команда правильная - пересоберите приложение

---

### ❌ Ошибка: "Port is already in use"

**Причина:** TimeWeb не передал переменную `$PORT`

**Решение:**
- Убедитесь, что в переменных окружения **НЕТ** переменной `PORT`
- TimeWeb сам передаст её при запуске контейнера

---

## 🔄 Автоматическое обновление

После настройки, любой `git push` в ветку `main` автоматически запустит новый деплой:

```bash
# Внесите изменения в код
git add .
git commit -m "Update content"
git push origin main

# TimeWeb автоматически:
# 1. Обнаружит коммит через webhook
# 2. Выполнит git pull
# 3. Соберет проект (npm run build)
# 4. Перезапустит сервер (npm start)
# 5. Обновит сайт (2-3 минуты)
```

---

## 📞 Поддержка

### Контакты компании:
- **Email:** Golden.House.Services@mail.ru
- **WhatsApp:** +7 (928) 195-88-85

### TimeWeb Cloud Support:
- **Документация:** https://timeweb.cloud/help/
- **Документация Next.js:** https://timeweb.cloud/docs/apps/deploying-frontend-apps/nextjs
- **Тикеты:** В личном кабинете TimeWeb Cloud

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу:

**https://tips95-golden-house-dccb.twc1.net** 🚀

При каждом `git push` сайт будет автоматически обновляться!
